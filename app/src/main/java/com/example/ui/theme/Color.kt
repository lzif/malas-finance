package com.example.ui.theme

import androidx.compose.ui.graphics.Color

val SoftBackground = Color(0xFFF3EEF4) // Pastel background
val White = Color(0xFFFFFFFF) // Surface/Cards
val ComponentBg = Color(0xFFF0F0F5) // Light gray component background (wallet, notes, amount)
val PrimaryBlue = Color(0xFF3B82F6) // Bright soft blue for submit button and OPER
val TextPrimary = Color(0xFF1E1E1E) // Dark Gray/Charcoal
val TextSecondary = Color(0xFF9094A6) // Medium Gray

val SoftRed = Color(0xFFFF6B6B) // Soft Red for CORE
val SoftYellow = Color(0xFFFFA726) // Soft Yellow/Orange for HOBBY
val MintGreen = Color(0xFF4ADE80) // Mint Green for VAULT

// Finance Categories mapping for the soft theme
val CoreColor = SoftRed
val OpsColor = PrimaryBlue
val HobbyColor = SoftYellow
val VaultColor = MintGreen
val TextGray = TextSecondary
val TextWhite = White // For text on bright primary buttons
val DarkGray = Color(0xFFE2E2E2) // Gray for dark background/borders if needed
val MediumGray = Color(0xFFE0E0E0)
val BorderGray = Color(0xFFE6E6EB)
val Black = TextPrimary // Used as main text

